import { DirectivenameDirective } from './directivename.directive';

describe('DirectivenameDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivenameDirective();
    expect(directive).toBeTruthy();
  });
});
