import { MasterDirective } from './master.directive';

describe('MasterDirective', () => {
  it('should create an instance', () => {
    const directive = new MasterDirective();
    expect(directive).toBeTruthy();
  });
});
